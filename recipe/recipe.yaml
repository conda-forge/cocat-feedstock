# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.3.0"

package:
  name: cocat
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/c/cocat/cocat-${{ version }}.tar.gz
    sha256: cc74ea02566e889adf878003cecee6d0f766949ec712ac8e2013c72cd134cf85

build:
  number: 0
  noarch: python
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - uv-build
  run:
    - python >=${{ python_min }}
    - pycrdt >=0.12.40,<0.13.0
    - pydantic >=2.12.0,<3.0.0

tests:
  - python:
      imports: cocat
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/SciQLop/cocat
  summary: CRDT-based tscat.

extra:
  recipe-maintainers:
    - davidbrochart
